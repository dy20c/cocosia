#common yml for official
server:
  port: 2020

spring:
  datasource:
    url: jdbc:mariadb://db-cocosia.mariadb.database.azure.com:3306/cocosia
    username: secret
    password: secret

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always